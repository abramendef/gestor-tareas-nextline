generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Task {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  completed   Boolean
  dueDate     DateTime
  comments    String?
  responsible String?
  tags        String?
  userId      String
}
